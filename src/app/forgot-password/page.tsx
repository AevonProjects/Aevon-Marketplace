export default function ForgotPasswordPage() {
  return (
    <main className="container flex min-h-[75vh] items-center justify-center py-12">
      <div className="card w-full max-w-md p-8">
        <h1 className="text-3xl font-black">Reset password</h1>
        <p className="mt-3 text-sm leading-6 text-zinc-400">
          Password reset email delivery will be connected in a later milestone. The route is reserved now so the account flow stays stable.
        </p>
      </div>
    </main>
  );
}
